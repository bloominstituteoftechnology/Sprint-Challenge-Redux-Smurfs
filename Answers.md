# 1) concat, map, filter, reduce
# 2) Store - holds the state, Actions - functions dispatched by store to the reducer, Reducer - takes state and action as parameters and returns an altered state (immutable)
#
#
#
#
#
#
