1. JS Pure functions: 

    Math.max(x, y, z);

    function pop(arr) {
        return arr.slice(0, -1);
    }

    function reverse(arr) {
        return arr.concat().reverse();
    }

We use Object.create();

2. 