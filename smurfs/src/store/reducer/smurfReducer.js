const initialState = {
  smurf: [],
  loading: false,
  errors: ""
};

export default function smurfReducer(state = initialState, action) {
  switch (action.type) {
    // case 'ACTION_TYPE':
    //   return code
    default:
      return state;
  }
}
