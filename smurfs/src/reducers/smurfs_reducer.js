import {GET_SMURFS,ADD_SMURF, UPDATE_SMURF, DELETE_SMURF, FETCHING, ERROR_GETTING_SMURFS} from '../actions/';

const initialState = {
    smurfs: [],
    error: null,
};

export const smurfs_reducer = (state = initialState, action) => {

    return true;
};
