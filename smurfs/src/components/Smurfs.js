import React, { Component } from 'react';

export const Smurfs = (props) => {

    return (
        <div>
            {/* {props.map((smurf) => {
                return <h1> {smurf} </h1>
            })} */}
            {console.log(props)}
                    </div>
    )
  }
